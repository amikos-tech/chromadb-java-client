---
phase: 02-api-coverage-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/tech/amikos/chromadb/v2/CollectionConfiguration.java
  - src/main/java/tech/amikos/chromadb/v2/Schema.java
  - src/main/java/tech/amikos/chromadb/v2/ChromaDtos.java
  - src/test/java/tech/amikos/chromadb/v2/ChromaDtosContractTest.java
  - src/test/java/tech/amikos/chromadb/v2/ChromaHttpCollectionTest.java
autonomous: true
requirements: [API-02]
user_setup: []
must_haves:
  truths:
    - Top-level `schema` remains canonical when both top-level and `configuration.schema` are present.
    - Unknown schema/config/CMEK keys are preserved in parse->serialize flows where parsing can proceed safely.
    - Collection mapping from server payloads retains non-lossy schema/config information for read paths.
  artifacts:
    - src/main/java/tech/amikos/chromadb/v2/CollectionConfiguration.java
    - src/main/java/tech/amikos/chromadb/v2/Schema.java
    - src/main/java/tech/amikos/chromadb/v2/ChromaDtos.java
    - src/test/java/tech/amikos/chromadb/v2/ChromaDtosContractTest.java
    - src/test/java/tech/amikos/chromadb/v2/ChromaHttpCollectionTest.java
  key_links:
    - "ChromaDtos parseConfiguration/toConfigurationMap round-trip"
    - "ChromaDtos parseSchema/toSchemaMap round-trip"
    - "API-02 schema/config/CMEK parity without lossy serialization"
---

<objective>
Implement API-02 schema/config/CMEK parity guarantees with non-lossy DTO mapping and explicit top-level schema precedence.

Purpose: prevent silent data loss during collection schema/config round-trips.
Output: passthrough-capable schema/config models + contract tests for unknown-key preservation.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api-coverage-completion/02-CONTEXT.md
@.planning/phases/02-api-coverage-completion/02-RESEARCH.md
@CLAUDE.md
@src/main/java/tech/amikos/chromadb/v2/CollectionConfiguration.java
@src/main/java/tech/amikos/chromadb/v2/Schema.java
@src/main/java/tech/amikos/chromadb/v2/ChromaDtos.java
@src/test/java/tech/amikos/chromadb/v2/ChromaDtosContractTest.java
@src/test/java/tech/amikos/chromadb/v2/ChromaHttpCollectionTest.java

<interfaces>
From `src/main/java/tech/amikos/chromadb/v2/CollectionConfiguration.java`:
```java
public final class CollectionConfiguration {
  public CollectionConfiguration.Builder schema(Schema schema);
  public Schema getSchema();
  public EmbeddingFunctionSpec getEmbeddingFunction();
}
```

From `src/main/java/tech/amikos/chromadb/v2/Schema.java`:
```java
public final class Schema {
  public ValueTypes getDefaults();
  public Map<String, ValueTypes> getKeys();
  public Cmek getCmek();
  public EmbeddingFunctionSpec getDefaultEmbeddingFunctionSpec();
}
```

From `src/main/java/tech/amikos/chromadb/v2/ChromaDtos.java`:
```java
static Map<String, Object> toConfigurationMap(CollectionConfiguration config);
static CollectionConfiguration parseConfiguration(Map<String, Object> configJson);
static Map<String, Object> toSchemaMap(Schema schema);
static Schema parseSchema(Map<String, Object> schemaJson);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add contract tests for unknown-key preservation and schema precedence</name>
  <files>src/test/java/tech/amikos/chromadb/v2/ChromaDtosContractTest.java, src/test/java/tech/amikos/chromadb/v2/ChromaHttpCollectionTest.java</files>
  <read_first>
    .planning/phases/02-api-coverage-completion/02-CONTEXT.md
    src/main/java/tech/amikos/chromadb/v2/ChromaDtos.java
    src/main/java/tech/amikos/chromadb/v2/Schema.java
    src/main/java/tech/amikos/chromadb/v2/CollectionConfiguration.java
    src/test/java/tech/amikos/chromadb/v2/ChromaDtosContractTest.java
    src/test/java/tech/amikos/chromadb/v2/ChromaHttpCollectionTest.java
  </read_first>
  <action>
    Add tests that define the exact API-02 contract:
    1) `toConfigurationMap(parseConfiguration(...))` keeps unknown top-level config keys.
    2) `toSchemaMap(parseSchema(...))` keeps unknown schema keys, including unknown-but-valid `cmek` provider entries, without corrupting known fields.
    3) Duplicate keys are typed-authoritative: known typed fields override same-key passthrough entries when re-serializing.
    4) `ChromaHttpCollection.from(...)` still prefers top-level `schema` over `configuration.schema` while preserving passthrough content from the canonical source.
    Use fixed test names containing `PreservesUnknown`, `TypedAuthoritative`, and `PrefersTopLevelSchema`.
  </action>
  <verify>
    <automated>mvn -q -Dtest=ChromaDtosContractTest,ChromaHttpCollectionTest test</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "PreservesUnknown|TypedAuthoritative|PrefersTopLevelSchema" src/test/java/tech/amikos/chromadb/v2/ChromaDtosContractTest.java src/test/java/tech/amikos/chromadb/v2/ChromaHttpCollectionTest.java` finds newly added contract tests.
    - `mvn -q -Dtest=ChromaDtosContractTest,ChromaHttpCollectionTest test` exits 0.
  </acceptance_criteria>
  <done>
    API-02 preservation/precedence behavior is encoded in failing-then-passing contract tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement passthrough-aware schema/config models and DTO mapping</name>
  <files>src/main/java/tech/amikos/chromadb/v2/CollectionConfiguration.java, src/main/java/tech/amikos/chromadb/v2/Schema.java, src/main/java/tech/amikos/chromadb/v2/ChromaDtos.java</files>
  <read_first>
    .planning/phases/02-api-coverage-completion/02-CONTEXT.md
    src/main/java/tech/amikos/chromadb/v2/CollectionConfiguration.java
    src/main/java/tech/amikos/chromadb/v2/Schema.java
    src/main/java/tech/amikos/chromadb/v2/ChromaDtos.java
    src/test/java/tech/amikos/chromadb/v2/ChromaDtosContractTest.java
    src/test/java/tech/amikos/chromadb/v2/ChromaHttpCollectionTest.java
  </read_first>
  <behavior>
    - Test 1: unknown config/schema/CMEK keys round-trip through parse->serialize.
    - Test 2: top-level schema stays canonical when both schema locations exist.
    - Test 3: typed known keys override passthrough duplicates during serialization.
  </behavior>
  <action>
    Introduce immutable passthrough maps with these concrete APIs:
    - `CollectionConfiguration`: add `private final Map<String, Object> passthrough`, `getPassthrough()`, and `Builder.passthrough(Map<String, Object>)`.
    - `Schema`: add `private final Map<String, Object> passthrough`, `getPassthrough()`, and `Builder.passthrough(Map<String, Object>)`.
    In `ChromaDtos`:
    - `parseConfiguration(...)`: extract unknown keys into configuration passthrough while keeping strict validation for known typed fields.
    - `toConfigurationMap(...)`: merge passthrough entries after typed fields, skipping passthrough keys that conflict with typed-known keys.
    - `parseSchema(...)`: extract unknown schema root keys into schema passthrough; for `schema.cmek`, keep known provider parsing for supported providers, and preserve unsupported provider entries in passthrough instead of silently dropping them.
    - `toSchemaMap(...)`: emit typed-known schema fields first, then passthrough keys not already emitted.
    Keep existing top-level-schema precedence behavior in collection mapping unchanged.
  </action>
  <verify>
    <automated>mvn -q -Dtest=ChromaDtosContractTest,ChromaHttpCollectionTest test && CHROMA_VERSION=1.5.5 mvn -q -Pintegration -Dtest=CollectionLifecycleIntegrationTest,SchemaAndQueryTextsIntegrationTest test</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "getPassthrough\\(|passthrough\\(" src/main/java/tech/amikos/chromadb/v2/CollectionConfiguration.java src/main/java/tech/amikos/chromadb/v2/Schema.java` confirms new passthrough API.
    - `rg -n "typed.*override|conflict|passthrough" src/main/java/tech/amikos/chromadb/v2/ChromaDtos.java` confirms explicit merge/conflict behavior.
    - `mvn -q -Dtest=ChromaDtosContractTest,ChromaHttpCollectionTest test` exits 0.
    - `CHROMA_VERSION=1.5.5 mvn -q -Pintegration -Dtest=CollectionLifecycleIntegrationTest,SchemaAndQueryTextsIntegrationTest test` exits 0.
  </acceptance_criteria>
  <done>
    Schema/config/CMEK mapping preserves unknown keys where possible without regressing strict validation of known fields.
  </done>
</task>

</tasks>

<verification>
<automated>mvn -q -Dtest=ChromaDtosContractTest,ChromaHttpCollectionTest test</automated>
<automated>CHROMA_VERSION=1.5.5 mvn -q -Pintegration -Dtest=CollectionLifecycleIntegrationTest,SchemaAndQueryTextsIntegrationTest test</automated>
</verification>

<success_criteria>
- API-02 collection schema/config/CMEK behavior is non-lossy for unknown keys where parsing can proceed.
- Top-level schema precedence remains canonical.
- Contract tests enforce mapping fidelity and prevent future lossy regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-coverage-completion/02-02-SUMMARY.md`
</output>
