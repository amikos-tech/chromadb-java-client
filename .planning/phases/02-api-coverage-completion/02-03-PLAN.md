---
phase: 02-api-coverage-completion
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/main/java/tech/amikos/chromadb/v2/ChromaHttpCollection.java
  - src/test/java/tech/amikos/chromadb/v2/ChromaHttpCollectionTest.java
  - src/test/java/tech/amikos/chromadb/v2/RecordOperationsIntegrationTest.java
  - src/test/java/tech/amikos/chromadb/v2/SchemaAndQueryTextsIntegrationTest.java
  - Makefile
autonomous: true
requirements: [API-03]
user_setup: []
must_haves:
  truths:
    - Query accepts both `queryTexts` and `queryEmbeddings` in one request flow.
    - When both inputs are present, embeddings are authoritative and text embedding is skipped.
    - `include` defaults remain server-authoritative when omitted; explicit includes are forwarded exactly.
    - Phase 2 parity checks are runnable against Chroma `1.5.5` and `1.3.7`.
  artifacts:
    - src/main/java/tech/amikos/chromadb/v2/ChromaHttpCollection.java
    - src/test/java/tech/amikos/chromadb/v2/ChromaHttpCollectionTest.java
    - src/test/java/tech/amikos/chromadb/v2/RecordOperationsIntegrationTest.java
    - src/test/java/tech/amikos/chromadb/v2/SchemaAndQueryTextsIntegrationTest.java
    - Makefile
  key_links:
    - "Collection.query builder setters -> QueryBuilderImpl.execute embedding resolution"
    - "Record/query request payload -> include/query_embeddings contract"
    - "API-03 parity tests -> CHROMA_VERSION 1.5.5 and 1.3.7 matrix runs"
---

<objective>
Implement API-03 query/get/add/update/upsert/delete parity details for mixed query inputs and include behavior, then lock parity with a reproducible two-version test gate.

Purpose: align Java client record-query behavior with locked parity target (Python/Go semantics) and keep verification reproducible.
Output: updated query builder semantics + contract/integration tests + matrix command target.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api-coverage-completion/02-CONTEXT.md
@.planning/phases/02-api-coverage-completion/02-RESEARCH.md
@.planning/phases/02-api-coverage-completion/02-VALIDATION.md
@CLAUDE.md
@src/main/java/tech/amikos/chromadb/v2/Collection.java
@src/main/java/tech/amikos/chromadb/v2/ChromaHttpCollection.java
@src/test/java/tech/amikos/chromadb/v2/ChromaHttpCollectionTest.java
@src/test/java/tech/amikos/chromadb/v2/RecordOperationsIntegrationTest.java
@src/test/java/tech/amikos/chromadb/v2/SchemaAndQueryTextsIntegrationTest.java
@Makefile

<interfaces>
From `src/main/java/tech/amikos/chromadb/v2/Collection.java`:
```java
interface QueryBuilder {
  QueryBuilder queryTexts(String... texts);
  QueryBuilder queryTexts(List<String> texts);
  QueryBuilder queryEmbeddings(float[]... embeddings);
  QueryBuilder queryEmbeddings(List<float[]> embeddings);
  QueryBuilder include(Include... include);
  QueryResult execute();
}
```

From `src/main/java/tech/amikos/chromadb/v2/ChromaHttpCollection.java`:
```java
private final class QueryBuilderImpl implements QueryBuilder {
  private List<float[]> queryEmbeddings;
  private List<String> queryTexts;
  public QueryResult execute() { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add contract/integration tests for mixed query inputs and include forwarding</name>
  <files>src/test/java/tech/amikos/chromadb/v2/ChromaHttpCollectionTest.java, src/test/java/tech/amikos/chromadb/v2/RecordOperationsIntegrationTest.java, src/test/java/tech/amikos/chromadb/v2/SchemaAndQueryTextsIntegrationTest.java</files>
  <read_first>
    .planning/phases/02-api-coverage-completion/02-CONTEXT.md
    src/main/java/tech/amikos/chromadb/v2/ChromaHttpCollection.java
    src/test/java/tech/amikos/chromadb/v2/ChromaHttpCollectionTest.java
    src/test/java/tech/amikos/chromadb/v2/RecordOperationsIntegrationTest.java
    src/test/java/tech/amikos/chromadb/v2/SchemaAndQueryTextsIntegrationTest.java
  </read_first>
  <action>
    Replace mixed-input rejection tests with parity tests that assert both setter orders are accepted:
    - `queryTexts(...).queryEmbeddings(...)` executes and uses provided embeddings.
    - `queryEmbeddings(...).queryTexts(...)` executes and still uses provided embeddings.
    Add WireMock payload assertions that:
    - when `include(...)` is omitted, the outgoing query JSON does not force client defaults;
    - when `include(...)` is provided, outgoing JSON include array matches caller-provided values exactly.
    Add one integration test that supplies both `queryTexts` and explicit `queryEmbeddings` and validates embedding-authoritative result selection.
  </action>
  <verify>
    <automated>mvn -q -Dtest=ChromaHttpCollectionTest,RecordOperationsIntegrationTest,SchemaAndQueryTextsIntegrationTest test</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "AllowsMixed|EmbeddingsAuthoritative|include.*omitted|include.*exact" src/test/java/tech/amikos/chromadb/v2/ChromaHttpCollectionTest.java src/test/java/tech/amikos/chromadb/v2/RecordOperationsIntegrationTest.java src/test/java/tech/amikos/chromadb/v2/SchemaAndQueryTextsIntegrationTest.java` returns new parity tests.
    - `mvn -q -Dtest=ChromaHttpCollectionTest,RecordOperationsIntegrationTest,SchemaAndQueryTextsIntegrationTest test` exits 0.
  </acceptance_criteria>
  <done>
    API-03 mixed-input/include expectations are test-locked before runtime behavior is updated.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement embeddings-authoritative mixed-input query behavior</name>
  <files>src/main/java/tech/amikos/chromadb/v2/ChromaHttpCollection.java</files>
  <read_first>
    .planning/phases/02-api-coverage-completion/02-CONTEXT.md
    src/main/java/tech/amikos/chromadb/v2/Collection.java
    src/main/java/tech/amikos/chromadb/v2/ChromaHttpCollection.java
    src/test/java/tech/amikos/chromadb/v2/ChromaHttpCollectionTest.java
  </read_first>
  <behavior>
    - Test 1: mixed query inputs are accepted in both setter orders.
    - Test 2: `queryEmbeddings` wins when both inputs are present.
    - Test 3: `queryTexts` are embedded only when embeddings are absent.
  </behavior>
  <action>
    In `QueryBuilderImpl`:
    - Remove setter-time mutual-exclusion checks between `queryTexts` and `queryEmbeddings`.
    - Keep `queryTexts` structural validation (`validateQueryTexts`) unchanged.
    - In `execute()`, resolve embeddings with this exact precedence:
      1) non-empty `queryEmbeddings` as-is;
      2) otherwise embed `queryTexts` when provided and non-empty;
      3) otherwise throw `IllegalArgumentException("queryEmbeddings must be provided")`.
    - Preserve current include serialization contract: `include == null` means no client-forced include defaults.
  </action>
  <verify>
    <automated>mvn -q -Dtest=ChromaHttpCollectionTest test && CHROMA_VERSION=1.5.5 mvn -q -Pintegration -Dtest=RecordOperationsIntegrationTest,SchemaAndQueryTextsIntegrationTest test</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "cannot set both queryTexts and queryEmbeddings" src/main/java/tech/amikos/chromadb/v2/ChromaHttpCollection.java` returns no matches.
    - `rg -n "resolvedEmbeddings|embedQueryTexts\\(" src/main/java/tech/amikos/chromadb/v2/ChromaHttpCollection.java` shows execute-time precedence logic.
    - `mvn -q -Dtest=ChromaHttpCollectionTest test` exits 0.
    - `CHROMA_VERSION=1.5.5 mvn -q -Pintegration -Dtest=RecordOperationsIntegrationTest,SchemaAndQueryTextsIntegrationTest test` exits 0.
  </acceptance_criteria>
  <done>
    Query builder behavior now matches locked parity semantics without regressing existing structural validation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add reproducible Phase 2 parity matrix command</name>
  <files>Makefile</files>
  <read_first>
    .planning/phases/02-api-coverage-completion/02-VALIDATION.md
    CLAUDE.md
    Makefile
  </read_first>
  <action>
    Add `test-phase-02-parity` target to `Makefile` with these exact commands in order:
    1) `mvn -q -Dtest=ChromaClientImplTest,ChromaHttpCollectionTest,ChromaDtosContractTest test`
    2) `CHROMA_VERSION=1.5.5 mvn -q -Pintegration -Dtest=TenantDatabaseIntegrationTest,CollectionLifecycleIntegrationTest,RecordOperationsIntegrationTest,SchemaAndQueryTextsIntegrationTest test`
    3) `CHROMA_VERSION=1.3.7 mvn -q -Pintegration -Dtest=TenantDatabaseIntegrationTest,CollectionLifecycleIntegrationTest,RecordOperationsIntegrationTest,SchemaAndQueryTextsIntegrationTest test`
    Keep target non-interactive and compatible with local + CI execution.
  </action>
  <verify>
    <automated>make -n test-phase-02-parity | rg -n "ChromaClientImplTest|CHROMA_VERSION=1.5.5|CHROMA_VERSION=1.3.7|RecordOperationsIntegrationTest"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "^test-phase-02-parity:" Makefile` returns one target definition.
    - `make -n test-phase-02-parity` prints the exact three commands listed in action.
    - Printed commands include both required versions (`1.5.5`, `1.3.7`).
  </acceptance_criteria>
  <done>
    Phase 2 parity gate is reproducible and explicitly enforces the locked two-version compatibility baseline.
  </done>
</task>

</tasks>

<verification>
<automated>mvn -q -Dtest=ChromaHttpCollectionTest test</automated>
<automated>CHROMA_VERSION=1.5.5 mvn -q -Pintegration -Dtest=RecordOperationsIntegrationTest,SchemaAndQueryTextsIntegrationTest test</automated>
<automated>make -n test-phase-02-parity</automated>
</verification>

<success_criteria>
- API-03 mixed-query and include behavior matches locked parity contract.
- Record-operation parity checks run reliably against Chroma `1.5.5` and `1.3.7`.
- Maintainers can execute one command target to reproduce Phase 2 verification gate.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-coverage-completion/02-03-SUMMARY.md`
</output>
