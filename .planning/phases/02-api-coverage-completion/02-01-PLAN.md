---
phase: 02-api-coverage-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/tech/amikos/chromadb/v2/ChromaClient.java
  - src/test/java/tech/amikos/chromadb/v2/ChromaClientImplTest.java
  - src/test/java/tech/amikos/chromadb/v2/TenantDatabaseIntegrationTest.java
autonomous: true
requirements: [API-01]
user_setup: []
must_haves:
  truths:
    - createTenant/createDatabase return response names when the server provides non-blank values.
    - createTenant/createDatabase fall back to request names only when response names are missing or blank.
    - getTenant/getDatabase/listDatabases remain strict on malformed payload fields.
  artifacts:
    - src/main/java/tech/amikos/chromadb/v2/ChromaClient.java
    - src/test/java/tech/amikos/chromadb/v2/ChromaClientImplTest.java
    - src/test/java/tech/amikos/chromadb/v2/TenantDatabaseIntegrationTest.java
  key_links:
    - "ChromaClient#createTenant/createDatabase -> ChromaDtos.TenantResponse/DatabaseResponse mapping"
    - "API-01 lifecycle parity: create/get/list/delete typed behavior"
---

<objective>
Implement API-01 lifecycle parity for tenant/database create/get/list/delete behavior with the locked create-only fallback policy.

Purpose: keep lifecycle reads strict while making create responses response-authoritative when valid.
Output: updated lifecycle mapping logic + unit/integration parity tests.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api-coverage-completion/02-CONTEXT.md
@.planning/phases/02-api-coverage-completion/02-RESEARCH.md
@CLAUDE.md
@src/main/java/tech/amikos/chromadb/v2/Client.java
@src/main/java/tech/amikos/chromadb/v2/ChromaClient.java
@src/test/java/tech/amikos/chromadb/v2/ChromaClientImplTest.java
@src/test/java/tech/amikos/chromadb/v2/TenantDatabaseIntegrationTest.java

<interfaces>
From `src/main/java/tech/amikos/chromadb/v2/Client.java`:
```java
Tenant createTenant(String name);
Tenant getTenant(String name);
Database createDatabase(String name);
Database getDatabase(String name);
List<Database> listDatabases();
void deleteDatabase(String name);
```

From `src/main/java/tech/amikos/chromadb/v2/ChromaClient.java` (`ChromaClientImpl`):
```java
public Tenant createTenant(String name)
public Tenant getTenant(String name)
public Database createDatabase(String name)
public Database getDatabase(String name)
public List<Database> listDatabases()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lifecycle contract tests for response-authoritative create fallback</name>
  <files>src/test/java/tech/amikos/chromadb/v2/ChromaClientImplTest.java, src/test/java/tech/amikos/chromadb/v2/TenantDatabaseIntegrationTest.java</files>
  <read_first>
    .planning/phases/02-api-coverage-completion/02-CONTEXT.md
    src/main/java/tech/amikos/chromadb/v2/ChromaClient.java
    src/test/java/tech/amikos/chromadb/v2/ChromaClientImplTest.java
    src/test/java/tech/amikos/chromadb/v2/TenantDatabaseIntegrationTest.java
  </read_first>
  <action>
    In `ChromaClientImplTest`, add explicit create-contract tests with these exact scenarios:
    1) `createTenant` returns server response name when JSON has non-blank `name`.
    2) `createTenant` falls back to request name when response `name` is null/missing/blank.
    3) `createDatabase` returns server response name when JSON has non-blank `name`.
    4) `createDatabase` falls back to request name when response `name` is null/missing/blank.
    Keep existing strict-read tests unchanged (`getTenant`, `getDatabase`, `listDatabases` malformed payload behavior must still throw).
    In `TenantDatabaseIntegrationTest`, add one assertion-focused test that create/get/list remain typed and non-blank across a full create->get->list flow.
  </action>
  <verify>
    <automated>mvn -q -Dtest=ChromaClientImplTest,TenantDatabaseIntegrationTest test</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "createTenant.*response|fallback|createDatabase.*response|fallback" src/test/java/tech/amikos/chromadb/v2/ChromaClientImplTest.java` returns new create-fallback test coverage.
    - `rg -n "create->get->list|create and get and list|non-blank" src/test/java/tech/amikos/chromadb/v2/TenantDatabaseIntegrationTest.java` returns the new lifecycle parity test.
    - `mvn -q -Dtest=ChromaClientImplTest,TenantDatabaseIntegrationTest test` exits 0.
  </acceptance_criteria>
  <done>
    API-01 create fallback and strict-read contract is fully encoded in tests before implementation changes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement create-only response-authoritative lifecycle mapping in ChromaClient</name>
  <files>src/main/java/tech/amikos/chromadb/v2/ChromaClient.java</files>
  <read_first>
    .planning/phases/02-api-coverage-completion/02-CONTEXT.md
    src/main/java/tech/amikos/chromadb/v2/Client.java
    src/main/java/tech/amikos/chromadb/v2/ChromaClient.java
    src/test/java/tech/amikos/chromadb/v2/ChromaClientImplTest.java
  </read_first>
  <behavior>
    - Test 1: `createTenant` returns response `name` when present, otherwise request name.
    - Test 2: `createDatabase` returns response `name` when present, otherwise request name.
    - Test 3: `getTenant/getDatabase/listDatabases` still throw strict deserialization errors for missing/blank names.
  </behavior>
  <action>
    Update `ChromaClientImpl#createTenant` and `#createDatabase` to capture typed response DTOs and compute the returned name as:
    - response `name.trim()` when response name is non-null and non-blank;
    - otherwise the normalized request name.
    Do not add fallback behavior to `getTenant`, `getDatabase`, or `listDatabases`; keep current `requireNonBlankField(...)` strictness there.
    Keep endpoint paths unchanged (`ChromaApiPaths.tenants()` and `ChromaApiPaths.databases(...)`).
  </action>
  <verify>
    <automated>mvn -q -Dtest=ChromaClientImplTest test && CHROMA_VERSION=1.5.5 mvn -q -Pintegration -Dtest=TenantDatabaseIntegrationTest test</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "createTenant\\(|createDatabase\\(" src/main/java/tech/amikos/chromadb/v2/ChromaClient.java` shows both methods now use typed response DTOs.
    - `rg -n "requireNonBlankField\\(\"tenant.name\"|requireNonBlankField\\(\"database.name\"" src/main/java/tech/amikos/chromadb/v2/ChromaClient.java` confirms strict reads remain.
    - `mvn -q -Dtest=ChromaClientImplTest test` exits 0.
    - `CHROMA_VERSION=1.5.5 mvn -q -Pintegration -Dtest=TenantDatabaseIntegrationTest test` exits 0.
  </acceptance_criteria>
  <done>
    Lifecycle create methods now match locked fallback semantics while read/list strictness stays fail-fast.
  </done>
</task>

</tasks>

<verification>
<automated>mvn -q -Dtest=ChromaClientImplTest test</automated>
<automated>CHROMA_VERSION=1.5.5 mvn -q -Pintegration -Dtest=TenantDatabaseIntegrationTest test</automated>
</verification>

<success_criteria>
- API-01 create/get/list/delete lifecycle behavior is typed and parity-locked for tenant/database operations.
- Create paths are response-authoritative with create-only fallback.
- Read/list paths stay strict on malformed payloads.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-coverage-completion/02-01-SUMMARY.md`
</output>
