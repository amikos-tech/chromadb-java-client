---
phase: 01-result-ergonomics-wheredocument
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/tech/amikos/chromadb/v2/ResultRow.java
  - src/main/java/tech/amikos/chromadb/v2/QueryResultRow.java
  - src/main/java/tech/amikos/chromadb/v2/ResultGroup.java
  - src/main/java/tech/amikos/chromadb/v2/ResultRowImpl.java
  - src/main/java/tech/amikos/chromadb/v2/QueryResultRowImpl.java
  - src/main/java/tech/amikos/chromadb/v2/ResultGroupImpl.java
  - src/test/java/tech/amikos/chromadb/v2/ResultRowTest.java
autonomous: true
requirements: [ERGO-01]

must_haves:
  truths:
    - "ResultRow interface exists with getId, getDocument, getMetadata, getEmbedding, getUri methods"
    - "QueryResultRow extends ResultRow and adds getDistance returning boxed Float"
    - "ResultGroup implements Iterable and provides get, size, isEmpty, stream, toList"
    - "ResultGroup.get(invalidIndex) throws IndexOutOfBoundsException"
    - "ResultRow fields return null when not included (no Optional, no exception)"
    - "Embedding arrays are defensively copied on construction and on every getEmbedding call"
    - "Metadata maps are unmodifiable"
  artifacts:
    - path: "src/main/java/tech/amikos/chromadb/v2/ResultRow.java"
      provides: "Public interface for row-based result access"
      exports: ["ResultRow"]
    - path: "src/main/java/tech/amikos/chromadb/v2/QueryResultRow.java"
      provides: "Public interface extending ResultRow with distance"
      exports: ["QueryResultRow"]
    - path: "src/main/java/tech/amikos/chromadb/v2/ResultGroup.java"
      provides: "Public generic interface for iterable result groups"
      exports: ["ResultGroup"]
    - path: "src/main/java/tech/amikos/chromadb/v2/ResultRowImpl.java"
      provides: "Package-private immutable implementation of ResultRow"
    - path: "src/main/java/tech/amikos/chromadb/v2/QueryResultRowImpl.java"
      provides: "Package-private immutable implementation of QueryResultRow"
    - path: "src/main/java/tech/amikos/chromadb/v2/ResultGroupImpl.java"
      provides: "Package-private implementation of ResultGroup backed by unmodifiable list"
    - path: "src/test/java/tech/amikos/chromadb/v2/ResultRowTest.java"
      provides: "Unit tests for all new types"
      min_lines: 100
  key_links:
    - from: "ResultRowImpl"
      to: "ResultRow"
      via: "implements"
      pattern: "class ResultRowImpl implements ResultRow"
    - from: "QueryResultRowImpl"
      to: "QueryResultRow"
      via: "implements"
      pattern: "class QueryResultRowImpl implements QueryResultRow"
    - from: "ResultGroupImpl"
      to: "ResultGroup"
      via: "implements"
      pattern: "class ResultGroupImpl.*implements ResultGroup"
---

<objective>
Create the ResultRow, QueryResultRow, and ResultGroup type hierarchy with package-private implementations and comprehensive unit tests.

Purpose: These types form the foundation for row-based result iteration (ERGO-01). They must exist before QueryResult/GetResult can be wired to use them (Plan 02).
Output: 6 new production files (3 interfaces, 3 impls) + 1 test file, all compiling and tested.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-result-ergonomics-wheredocument/01-CONTEXT.md
@.planning/phases/01-result-ergonomics-wheredocument/01-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/main/java/tech/amikos/chromadb/v2/Include.java:
```java
public enum Include {
    EMBEDDINGS("embeddings"),
    DOCUMENTS("documents"),
    METADATAS("metadatas"),
    DISTANCES("distances"),
    URIS("uris");
}
```

From src/main/java/tech/amikos/chromadb/v2/GetResultImpl.java (immutability patterns):
```java
// Defensive copy patterns to follow:
private static <T> List<T> immutableList(List<T> list) {
    if (list == null) return null;
    return Collections.unmodifiableList(new ArrayList<T>(list));
}
private static List<float[]> immutableEmbeddings(List<float[]> embeddings) {
    if (embeddings == null) return null;
    List<float[]> copy = new ArrayList<float[]>(embeddings.size());
    for (float[] embedding : embeddings) {
        copy.add(embedding == null ? null : Arrays.copyOf(embedding, embedding.length));
    }
    return Collections.unmodifiableList(copy);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ResultRow, QueryResultRow, ResultGroup interfaces and package-private implementations</name>
  <files>
    src/main/java/tech/amikos/chromadb/v2/ResultRow.java
    src/main/java/tech/amikos/chromadb/v2/QueryResultRow.java
    src/main/java/tech/amikos/chromadb/v2/ResultGroup.java
    src/main/java/tech/amikos/chromadb/v2/ResultRowImpl.java
    src/main/java/tech/amikos/chromadb/v2/QueryResultRowImpl.java
    src/main/java/tech/amikos/chromadb/v2/ResultGroupImpl.java
    src/test/java/tech/amikos/chromadb/v2/ResultRowTest.java
  </files>
  <read_first>
    src/main/java/tech/amikos/chromadb/v2/GetResultImpl.java
    src/main/java/tech/amikos/chromadb/v2/QueryResultImpl.java
    src/main/java/tech/amikos/chromadb/v2/Include.java
    src/test/java/tech/amikos/chromadb/v2/WhereTest.java
  </read_first>
  <behavior>
    - Test: ResultRowImpl returns null for all optional fields when constructed with nulls
    - Test: ResultRowImpl.getEmbedding() returns defensive copy (mutating returned array does not affect next call)
    - Test: ResultRowImpl.getMetadata() returns unmodifiable map (put throws UnsupportedOperationException)
    - Test: ResultRowImpl stores and returns all fields correctly (id, document, metadata, embedding, uri)
    - Test: QueryResultRowImpl.getDistance() returns boxed Float (null when not included)
    - Test: QueryResultRowImpl inherits all ResultRow methods correctly
    - Test: ResultGroupImpl.get(0) returns first element
    - Test: ResultGroupImpl.get(invalidIndex) throws IndexOutOfBoundsException
    - Test: ResultGroupImpl.size() returns correct count
    - Test: ResultGroupImpl.isEmpty() returns true for empty group, false for non-empty
    - Test: ResultGroupImpl.stream() returns stream of correct size and elements
    - Test: ResultGroupImpl.toList() returns unmodifiable list
    - Test: ResultGroupImpl implements Iterable (enhanced for-loop works)
  </behavior>
  <action>
    **Create `ResultRow.java`** (public interface, per D-01, D-03, D-05):
    ```java
    package tech.amikos.chromadb.v2;
    import java.util.Map;
    public interface ResultRow {
        String getId();
        String getDocument();
        Map<String, Object> getMetadata();
        float[] getEmbedding();
        String getUri();
    }
    ```

    **Create `QueryResultRow.java`** (public interface, per D-02):
    ```java
    package tech.amikos.chromadb.v2;
    public interface QueryResultRow extends ResultRow {
        Float getDistance();
    }
    ```

    **Create `ResultGroup.java`** (public interface, per D-06, D-07, D-08, D-09):
    ```java
    package tech.amikos.chromadb.v2;
    import java.util.Iterator;
    import java.util.List;
    import java.util.stream.Stream;
    public interface ResultGroup<R extends ResultRow> extends Iterable<R> {
        R get(int index);
        int size();
        boolean isEmpty();
        Stream<R> stream();
        List<R> toList();
    }
    ```

    **Create `ResultRowImpl.java`** (package-private final class):
    - Constructor: `ResultRowImpl(String id, String document, Map<String, Object> metadata, float[] embedding, String uri)`
    - Store `metadata` as `metadata == null ? null : Collections.unmodifiableMap(new LinkedHashMap<String, Object>(metadata))`
    - Store `embedding` as `embedding == null ? null : Arrays.copyOf(embedding, embedding.length)`
    - `getEmbedding()` returns `embedding == null ? null : Arrays.copyOf(embedding, embedding.length)` (defensive copy every call)
    - Other getters return stored field directly (String and Map are already immutable/unmodifiable)
    - Per D-04: all fields may be null (not included via Include)

    **Create `QueryResultRowImpl.java`** (package-private final class):
    - Constructor: `QueryResultRowImpl(String id, String document, Map<String, Object> metadata, float[] embedding, String uri, Float distance)`
    - Delegates to a private `ResultRowImpl` for the base fields (composition, not inheritance) OR duplicates the fields. Composition preferred to avoid code duplication.
    - `getDistance()` returns boxed `Float` (null when DISTANCES not included, per D-04)
    - All `ResultRow` methods delegate to the inner `ResultRowImpl`

    **Create `ResultGroupImpl.java`** (package-private final generic class):
    - Constructor: `ResultGroupImpl(List<R> rows)` where `R extends ResultRow`
    - Store as `this.rows = Collections.unmodifiableList(new ArrayList<R>(rows))`
    - `get(int index)` delegates to `rows.get(index)` which throws IndexOutOfBoundsException per D-08
    - `size()` returns `rows.size()`
    - `isEmpty()` returns `rows.isEmpty()`
    - `iterator()` returns `rows.iterator()`
    - `stream()` returns `rows.stream()` (Java 8 safe)
    - `toList()` returns `rows` (already unmodifiable, no copy needed)

    **Create `ResultRowTest.java`** with JUnit 4 tests covering all behaviors listed above. Since impls are package-private, the test file must be in package `tech.amikos.chromadb.v2`. Use direct constructor calls (package-private access). Follow the assertion style from `WhereTest.java` (assertEquals, fail, try/catch for expected exceptions).
  </action>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/chromadb-java-client && mvn test -Dtest=ResultRowTest -pl . 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/main/java/tech/amikos/chromadb/v2/ResultRow.java contains `public interface ResultRow`
    - src/main/java/tech/amikos/chromadb/v2/ResultRow.java contains `String getId();`
    - src/main/java/tech/amikos/chromadb/v2/ResultRow.java contains `String getDocument();`
    - src/main/java/tech/amikos/chromadb/v2/ResultRow.java contains `Map<String, Object> getMetadata();`
    - src/main/java/tech/amikos/chromadb/v2/ResultRow.java contains `float[] getEmbedding();`
    - src/main/java/tech/amikos/chromadb/v2/ResultRow.java contains `String getUri();`
    - src/main/java/tech/amikos/chromadb/v2/QueryResultRow.java contains `public interface QueryResultRow extends ResultRow`
    - src/main/java/tech/amikos/chromadb/v2/QueryResultRow.java contains `Float getDistance();`
    - src/main/java/tech/amikos/chromadb/v2/ResultGroup.java contains `public interface ResultGroup<R extends ResultRow> extends Iterable<R>`
    - src/main/java/tech/amikos/chromadb/v2/ResultGroup.java contains `R get(int index);`
    - src/main/java/tech/amikos/chromadb/v2/ResultGroup.java contains `Stream<R> stream();`
    - src/main/java/tech/amikos/chromadb/v2/ResultGroup.java contains `List<R> toList();`
    - src/main/java/tech/amikos/chromadb/v2/ResultRowImpl.java contains `final class ResultRowImpl implements ResultRow`
    - src/main/java/tech/amikos/chromadb/v2/ResultRowImpl.java contains `Arrays.copyOf(embedding, embedding.length)`
    - src/main/java/tech/amikos/chromadb/v2/ResultRowImpl.java contains `Collections.unmodifiableMap`
    - src/main/java/tech/amikos/chromadb/v2/QueryResultRowImpl.java contains `final class QueryResultRowImpl implements QueryResultRow`
    - src/main/java/tech/amikos/chromadb/v2/QueryResultRowImpl.java contains `Float getDistance()`
    - src/main/java/tech/amikos/chromadb/v2/ResultGroupImpl.java contains `final class ResultGroupImpl`
    - src/main/java/tech/amikos/chromadb/v2/ResultGroupImpl.java contains `implements ResultGroup`
    - src/main/java/tech/amikos/chromadb/v2/ResultGroupImpl.java contains `Collections.unmodifiableList`
    - src/test/java/tech/amikos/chromadb/v2/ResultRowTest.java contains `testResultRowFieldsNullWhenNotIncluded`
    - src/test/java/tech/amikos/chromadb/v2/ResultRowTest.java contains `testEmbeddingDefensiveCopy`
    - src/test/java/tech/amikos/chromadb/v2/ResultRowTest.java contains `testResultGroupGetThrowsOnBadIndex`
    - `mvn test -Dtest=ResultRowTest` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All 6 production files compile. ResultRowTest passes with all tests green. Interfaces are public, implementations are package-private. Defensive copy on embedding, unmodifiable map on metadata, null for non-included fields.</done>
</task>

</tasks>

<verification>
- `mvn test -Dtest=ResultRowTest` passes
- `mvn clean compile` succeeds (no compilation errors in new files)
- All interfaces are public, all impls are package-private (no `public` keyword on impl class declarations)
</verification>

<success_criteria>
- ResultRow, QueryResultRow, ResultGroup interfaces exist as public types in tech.amikos.chromadb.v2
- Package-private implementations pass all unit tests
- Defensive copies verified for float[] embedding arrays
- Unmodifiable maps verified for metadata
- IndexOutOfBoundsException verified for invalid ResultGroup.get() calls
- Null returns verified for non-included fields
</success_criteria>

<output>
After completion, create `.planning/phases/01-result-ergonomics-wheredocument/01-01-SUMMARY.md`
</output>
