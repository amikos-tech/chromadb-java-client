---
phase: 01-transport-auth-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/tech/amikos/chromadb/v2/ChromaClient.java
  - src/main/java/tech/amikos/chromadb/v2/ChromaApiClient.java
  - src/test/java/tech/amikos/chromadb/v2/ChromaClientBuilderTest.java
  - src/test/java/tech/amikos/chromadb/v2/ChromaApiClientTest.java
autonomous: true
requirements: [AUTH-01, AUTH-03]
user_setup: []
must_haves:
  truths:
    - Exactly one auth strategy can be configured per builder instance.
    - Conflicting auth headers cannot be supplied via defaultHeaders.
    - Convenience auth setters route through one canonical auth pathway.
  artifacts:
    - src/main/java/tech/amikos/chromadb/v2/ChromaClient.java
    - src/test/java/tech/amikos/chromadb/v2/ChromaClientBuilderTest.java
    - src/test/java/tech/amikos/chromadb/v2/ChromaApiClientTest.java
  key_links:
    - AUTH-01
    - AUTH-03
---

# Phase 1 Plan 01: Enforce Repo-Wide Auth Configuration Boundary

## Objective
- **What:** Implement the single-auth configuration contract and remove ambiguous auth/header states.
- **Why:** AUTH-01 and AUTH-03 require predictable auth application and actionable validation behavior.
- **Output:** Builder/auth APIs enforce one-strategy policy with explicit errors and test coverage.

## Context
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-transport-auth-hardening/01-CONTEXT.md
@.planning/phases/01-transport-auth-hardening/01-RESEARCH.md

## Tasks

<task type="code">
  <name>Enforce single-auth strategy invariant in builders</name>
  <read_first>
    src/main/java/tech/amikos/chromadb/v2/ChromaClient.java
    src/main/java/tech/amikos/chromadb/v2/AuthProvider.java
    src/main/java/tech/amikos/chromadb/v2/TokenAuth.java
    src/main/java/tech/amikos/chromadb/v2/BasicAuth.java
    src/main/java/tech/amikos/chromadb/v2/ChromaTokenAuth.java
    src/test/java/tech/amikos/chromadb/v2/ChromaClientBuilderTest.java
  </read_first>
  <files>
    src/main/java/tech/amikos/chromadb/v2/ChromaClient.java
    src/test/java/tech/amikos/chromadb/v2/ChromaClientBuilderTest.java
  </files>
  <action>
    Refactor builder auth setters (`auth(...)`, `apiKey(...)`, and cloud equivalents) to write into one canonical auth slot and enforce exactly-one strategy semantics. Throw a clear failure when a second auth strategy is set. Keep a final build-time invariant validation so partially configured builders cannot bypass constraints.
  </action>
  <acceptance_criteria>
    - Builder tests assert second auth-setter call fails with explicit, actionable message.
    - Convenience setters still function but map to canonical auth path.
    - `mvn -Dtest=ChromaClientBuilderTest test` exits 0.
  </acceptance_criteria>
  <verify>
    mvn -Dtest=ChromaClientBuilderTest test
  </verify>
  <done>
    Auth configuration is unambiguous and enforced by construction-time and build-time checks.
  </done>
</task>

<task type="code">
  <name>Reject auth-conflicting default headers with guided errors</name>
  <read_first>
    src/main/java/tech/amikos/chromadb/v2/ChromaClient.java
    src/main/java/tech/amikos/chromadb/v2/ChromaApiClient.java
    src/test/java/tech/amikos/chromadb/v2/ChromaApiClientTest.java
    src/test/java/tech/amikos/chromadb/v2/ChromaClientBuilderTest.java
  </read_first>
  <files>
    src/main/java/tech/amikos/chromadb/v2/ChromaClient.java
    src/test/java/tech/amikos/chromadb/v2/ChromaApiClientTest.java
    src/test/java/tech/amikos/chromadb/v2/ChromaClientBuilderTest.java
  </files>
  <action>
    Block `defaultHeaders` from carrying `Authorization` or `X-Chroma-Token` and raise an error that explicitly tells users to configure credentials through `auth(...)`. Keep non-auth default headers supported unchanged.
  </action>
  <acceptance_criteria>
    - Tests assert conflicting header keys are rejected with message containing `auth(...)` guidance.
    - Tests assert non-auth default headers are still applied.
    - `mvn -Dtest=ChromaApiClientTest,ChromaClientBuilderTest test` exits 0.
  </acceptance_criteria>
  <verify>
    mvn -Dtest=ChromaApiClientTest,ChromaClientBuilderTest test
  </verify>
  <done>
    Default-header boundary is explicit and cannot shadow or conflict with auth providers.
  </done>
</task>

## Verification
- `mvn -Dtest=ChromaClientBuilderTest,ChromaApiClientTest test`

## Success Criteria
- [ ] Auth configuration ambiguity is eliminated in builder API surface.
- [ ] Conflicting auth headers in default headers are rejected with actionable guidance.
