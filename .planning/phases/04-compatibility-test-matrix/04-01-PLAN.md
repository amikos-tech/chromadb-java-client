---
phase: 04-compatibility-test-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/java/tech/amikos/chromadb/v2/AbstractChromaIntegrationTest.java
  - Makefile
  - .github/workflows/integration-test.yml
autonomous: true
requirements: [QLTY-01]

must_haves:
  truths:
    - "Maintainer can run `make test-matrix` and it sequentially tests all 3 pinned Chroma versions (1.0.0, 1.3.7, 1.5.5)"
    - "Integration tests that require a minimum Chroma version are skipped (not failed) when running against an older version"
    - "Container startup failures cause test failure (AssertionError), not silent skip"
    - "CI runs 5 matrix cells: JDK 8 x 3 Chroma versions + JDK 11/17 x 1.5.5 only"
    - "All CI matrix cells must pass for PR merge (no advisory-only cells)"
  artifacts:
    - path: "src/test/java/tech/amikos/chromadb/v2/AbstractChromaIntegrationTest.java"
      provides: "assumeMinVersion() helper, compareVersions() utility, fail-fast container startup"
      contains: "assumeMinVersion"
    - path: "Makefile"
      provides: "CHROMA_MATRIX_VERSIONS variable and test-matrix target"
      contains: "CHROMA_MATRIX_VERSIONS"
    - path: ".github/workflows/integration-test.yml"
      provides: "Matrix CI workflow with unit-tests, integration-tests, and v2-integration-test jobs"
      contains: "strategy"
  key_links:
    - from: "Makefile"
      to: "AbstractChromaIntegrationTest"
      via: "CHROMA_VERSION env var consumed by configuredChromaVersion()"
      pattern: "CHROMA_VERSION=.*mvn"
    - from: ".github/workflows/integration-test.yml"
      to: "AbstractChromaIntegrationTest"
      via: "CHROMA_VERSION env in matrix steps"
      pattern: "CHROMA_VERSION.*matrix.chroma-version"
---

<objective>
Implement a reproducible multi-version test matrix for Chroma versions (1.0.0, 1.3.7, 1.5.5) with JDK coverage (8, 11, 17). Add version-conditional test skip infrastructure, convert container startup failures from skip to fail-fast, create a local `make test-matrix` target, and expand the CI workflow to a 5-cell matrix.

Purpose: Delivers QLTY-01 — maintainer can run unit + integration tests against supported Chroma versions with reproducible pass/fail behavior.
Output: Updated AbstractChromaIntegrationTest with version helpers, Makefile with test-matrix target, GitHub Actions matrix workflow.
</objective>

<execution_context>
@/Users/tazarov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-compatibility-test-matrix/04-CONTEXT.md
@.planning/phases/04-compatibility-test-matrix/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add version-skip infrastructure and fail-fast container startup to AbstractChromaIntegrationTest</name>
  <files>src/test/java/tech/amikos/chromadb/v2/AbstractChromaIntegrationTest.java</files>
  <read_first>
    - src/test/java/tech/amikos/chromadb/v2/AbstractChromaIntegrationTest.java
    - .planning/phases/04-compatibility-test-matrix/04-RESEARCH.md (Pattern 2 and Pitfall 3 sections)
  </read_first>
  <action>
Modify `AbstractChromaIntegrationTest.java` with three changes:

**1. Update DEFAULT_CHROMA_VERSION constant:**
Change line `private static final String DEFAULT_CHROMA_VERSION = "1.5.2";` to:
```java
private static final String DEFAULT_CHROMA_VERSION = "1.5.5";
```

**2. Add version comparison and skip helper methods** (add after the `configuredChromaVersion()` method, before `embedding()`):

```java
/**
 * Skips the current test if the configured Chroma version is below {@code minVersion}.
 * Uses JUnit 4 {@link Assume#assumeTrue} so the test is marked as "skipped", not "failed".
 *
 * @param minVersion minimum required version in dotted format (e.g. "1.3.0")
 */
protected static void assumeMinVersion(String minVersion) {
    Assume.assumeTrue(
        "Skipping: requires Chroma >= " + minVersion
            + ", currently running " + configuredChromaVersion(),
        compareVersions(configuredChromaVersion(), minVersion) >= 0
    );
}

/**
 * Compares two dotted-version strings segment by segment.
 * Returns negative if v1 &lt; v2, zero if equal, positive if v1 &gt; v2.
 */
static int compareVersions(String v1, String v2) {
    String[] parts1 = v1.split("\\.");
    String[] parts2 = v2.split("\\.");
    int len = Math.max(parts1.length, parts2.length);
    for (int i = 0; i < len; i++) {
        int n1 = i < parts1.length ? Integer.parseInt(parts1[i]) : 0;
        int n2 = i < parts2.length ? Integer.parseInt(parts2[i]) : 0;
        if (n1 != n2) return Integer.compare(n1, n2);
    }
    return 0;
}
```

**3. Change container startup failure from skip to fail-fast** in the `setUp()` method. Replace this block:
```java
Assume.assumeTrue(
        "Skipping integration tests because Chroma Testcontainer failed to start"
                + " (image tag: " + configuredChromaVersion() + ")",
        CHROMA_STARTUP_FAILURE == null
);
```
with:
```java
if (CHROMA_STARTUP_FAILURE != null) {
    throw new AssertionError(
        "Chroma container failed to start for version "
            + configuredChromaVersion()
            + " -- failing test (not skipping)",
        CHROMA_STARTUP_FAILURE);
}
```

The `Assume` import can remain since `assumeMinVersion()` still uses it.
  </action>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/chromadb-java-client && mvn -q compile -pl . test-compile 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AbstractChromaIntegrationTest.java contains `DEFAULT_CHROMA_VERSION = "1.5.5"`
    - AbstractChromaIntegrationTest.java contains `protected static void assumeMinVersion(String minVersion)`
    - AbstractChromaIntegrationTest.java contains `static int compareVersions(String v1, String v2)`
    - AbstractChromaIntegrationTest.java contains `throw new AssertionError(` in setUp() instead of `Assume.assumeTrue` for startup failure
    - AbstractChromaIntegrationTest.java does NOT contain `Assume.assumeTrue` with `CHROMA_STARTUP_FAILURE`
    - Test compilation succeeds with `mvn test-compile`
  </acceptance_criteria>
  <done>AbstractChromaIntegrationTest has assumeMinVersion() helper, compareVersions() utility, DEFAULT_CHROMA_VERSION updated to 1.5.5, and container startup failures throw AssertionError instead of being silently skipped.</done>
</task>

<task type="auto">
  <name>Task 2: Add CHROMA_MATRIX_VERSIONS variable and test-matrix target to Makefile</name>
  <files>Makefile</files>
  <read_first>
    - Makefile
    - .planning/phases/04-compatibility-test-matrix/04-RESEARCH.md (Makefile test-matrix target section)
  </read_first>
  <action>
Modify the `Makefile` with two changes:

**1. Replace the empty `CHROMA_VERSIONS` variable** (currently line 7: `CHROMA_VERSIONS :=`) with the populated matrix variable:
```makefile
CHROMA_MATRIX_VERSIONS := 1.0.0 1.3.7 1.5.5
```

**2. Add the `test-matrix` target** after the `test-integration` target (after line 63) and before the `test-phase-02-parity` target. Insert:

```makefile
.PHONY: test-matrix
test-matrix: check-tools ## Run full unit + integration test matrix across all pinned Chroma versions
	@echo "$(BLUE)Running unit tests...$(NC)"
	$(MAVEN) --batch-mode test
	@echo "$(BLUE)Running integration tests across Chroma versions: $(CHROMA_MATRIX_VERSIONS)$(NC)"
	@set -e; for v in $(CHROMA_MATRIX_VERSIONS); do \
	  echo "$(BLUE)  Testing with Chroma $$v...$(NC)"; \
	  CHROMA_VERSION=$$v $(MAVEN) --batch-mode -Pintegration test || exit $$?; \
	done
	@echo "$(GREEN)test-matrix complete$(NC)"
```

Key details:
- `set -e` ensures loop exits on first failure (no silent swallowing of non-zero exit codes)
- `|| exit $$?` provides explicit exit code propagation as a safety belt
- Uses `$(MAVEN)` variable and `--batch-mode` flag consistent with existing targets
- Target depends on `check-tools` like all other test targets
- Help comment (`##`) follows existing Makefile convention

Also add a comment above `test-phase-02-parity` noting that `test-matrix` is the canonical matrix target:
```makefile
# NOTE: test-matrix is the canonical version-matrix target. test-phase-02-parity is kept for backward compatibility.
```
  </action>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/chromadb-java-client && grep -c "CHROMA_MATRIX_VERSIONS" Makefile && grep -c "test-matrix" Makefile</automated>
  </verify>
  <acceptance_criteria>
    - Makefile contains `CHROMA_MATRIX_VERSIONS := 1.0.0 1.3.7 1.5.5`
    - Makefile contains `.PHONY: test-matrix`
    - Makefile contains `test-matrix: check-tools`
    - Makefile contains `set -e; for v in $(CHROMA_MATRIX_VERSIONS)`
    - Makefile contains `CHROMA_VERSION=$$v $(MAVEN) --batch-mode -Pintegration test`
    - Makefile contains `|| exit $$?`
    - The old `CHROMA_VERSIONS :=` line is replaced (not duplicated)
    - `make help` output includes `test-matrix` (via the `##` comment pattern)
  </acceptance_criteria>
  <done>Makefile has centralized CHROMA_MATRIX_VERSIONS variable with 3 pinned versions and a test-matrix target that runs unit tests then integration tests sequentially across all versions with proper error propagation.</done>
</task>

<task type="auto">
  <name>Task 3: Replace CI workflow with matrix-parameterized jobs</name>
  <files>.github/workflows/integration-test.yml</files>
  <read_first>
    - .github/workflows/integration-test.yml
    - .planning/phases/04-compatibility-test-matrix/04-RESEARCH.md (Pattern 3 and full workflow example)
  </read_first>
  <action>
Replace the entire contents of `.github/workflows/integration-test.yml` with a 3-job matrix workflow:

```yaml
name: Integration test

on:
  pull_request:
    branches:
      - main
      - "**"

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        java: ['8', '11', '17']
    name: Unit tests (JDK ${{ matrix.java }})
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK ${{ matrix.java }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ matrix.java }}
          distribution: 'temurin'
          cache: maven
      - name: Run unit tests
        run: mvn --batch-mode test
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          COHERE_API_KEY: ${{ secrets.COHERE_API_KEY }}
          HF_API_KEY: ${{ secrets.HF_API_KEY }}

  integration-tests:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        chroma-version: ['1.0.0', '1.3.7', '1.5.5']
        java: ['8']
        include:
          - java: '11'
            chroma-version: '1.5.5'
          - java: '17'
            chroma-version: '1.5.5'
    name: Integration tests (JDK ${{ matrix.java }}, Chroma ${{ matrix.chroma-version }})
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK ${{ matrix.java }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ matrix.java }}
          distribution: 'temurin'
          cache: maven
      - name: Run integration tests
        run: mvn --batch-mode -Pintegration test
        env:
          CHROMA_VERSION: ${{ matrix.chroma-version }}

  v2-integration-test:
    runs-on: ubuntu-latest
    name: Cloud parity tests
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 8
        uses: actions/setup-java@v4
        with:
          java-version: '8'
          distribution: 'temurin'
          cache: maven
      - name: Run v2 integration tests
        run: mvn --batch-mode test -Pintegration
        env:
          CHROMA_VERSION: '1.5.5'
          CHROMA_API_KEY: ${{ secrets.CHROMA_API_KEY }}
          CHROMA_TENANT: ${{ secrets.CHROMA_TENANT }}
          CHROMA_DATABASE: ${{ secrets.CHROMA_DATABASE }}
```

Key changes from the existing workflow:
- Upgraded `actions/checkout` from v3 to v4
- Upgraded `actions/setup-java` from v3 to v4
- Replaced deprecated `distribution: 'adopt'` with `distribution: 'temurin'`
- Split single `integration-test` job into `unit-tests` (3 JDKs) + `integration-tests` (5-cell matrix)
- Unit tests run across JDK 8, 11, 17 with no Docker/Chroma dependency
- Integration tests: JDK 8 x (1.0.0, 1.3.7, 1.5.5) + JDK 11 x 1.5.5 + JDK 17 x 1.5.5 = 5 cells
- `fail-fast: false` on both matrix jobs so all cells run to completion
- No `continue-on-error` — all cells are required
- Cloud parity job (`v2-integration-test`) preserved as separate job with updated Chroma version 1.5.5
- API key secrets passed to unit-tests job (for embedding function tests that need them)
  </action>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/chromadb-java-client && grep -c "fail-fast: false" .github/workflows/integration-test.yml && grep -c "temurin" .github/workflows/integration-test.yml && grep -c "actions/checkout@v4" .github/workflows/integration-test.yml && grep "1.0.0" .github/workflows/integration-test.yml | head -1 && grep "1.3.7" .github/workflows/integration-test.yml | head -1 && grep "1.5.5" .github/workflows/integration-test.yml | head -1</automated>
  </verify>
  <acceptance_criteria>
    - integration-test.yml contains exactly 3 jobs: `unit-tests`, `integration-tests`, `v2-integration-test`
    - integration-test.yml contains `fail-fast: false` (appears twice, once per matrix job)
    - integration-test.yml contains `distribution: 'temurin'` (not `adopt`)
    - integration-test.yml contains `actions/checkout@v4` (not v3)
    - integration-test.yml contains `actions/setup-java@v4` (not v3)
    - integration-test.yml contains `chroma-version: ['1.0.0', '1.3.7', '1.5.5']`
    - integration-test.yml contains `java: ['8', '11', '17']` in unit-tests job
    - integration-test.yml contains two `include:` entries for JDK 11 and 17 with chroma-version 1.5.5
    - integration-test.yml does NOT contain `distribution: 'adopt'`
    - integration-test.yml does NOT contain `actions/checkout@v3` or `actions/setup-java@v3`
  </acceptance_criteria>
  <done>CI workflow has 3 jobs: unit-tests across 3 JDKs, integration-tests across 5 matrix cells (JDK 8 x 3 Chroma + JDK 11/17 x latest), and cloud parity tests. All cells required, no fail-fast, using temurin distribution and v4 actions.</done>
</task>

</tasks>

<verification>
1. `mvn test-compile` passes (AbstractChromaIntegrationTest compiles with new methods)
2. `grep "assumeMinVersion" src/test/java/tech/amikos/chromadb/v2/AbstractChromaIntegrationTest.java` returns a match
3. `grep "AssertionError" src/test/java/tech/amikos/chromadb/v2/AbstractChromaIntegrationTest.java` returns a match (fail-fast)
4. `grep "CHROMA_MATRIX_VERSIONS" Makefile` returns a match
5. `grep "test-matrix" Makefile` returns a match
6. `grep "fail-fast: false" .github/workflows/integration-test.yml` returns matches
7. `grep "temurin" .github/workflows/integration-test.yml` returns matches
8. `grep "adopt" .github/workflows/integration-test.yml` returns NO matches
</verification>

<success_criteria>
- AbstractChromaIntegrationTest has assumeMinVersion(), compareVersions(), fail-fast startup, and DEFAULT_CHROMA_VERSION = 1.5.5
- Makefile has CHROMA_MATRIX_VERSIONS := 1.0.0 1.3.7 1.5.5 and test-matrix target with proper error propagation
- CI workflow has 3 jobs with 5 integration-test matrix cells, all using temurin/v4, no fail-fast
- All changes compile cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-compatibility-test-matrix/04-01-SUMMARY.md`
</output>
